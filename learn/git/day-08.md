# Day 08 — Connecting to a Remote

origin is the default name for the main remote repository.
git remote add origin <url> links local repo to GitHub.
git remote -v shows fetch and push URLs.
git push -u origin master sets upstream tracking.