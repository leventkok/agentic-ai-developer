# Day 11 — Cloning a Repository

git clone <url> creates a full local copy with history and origin remote.
Use clone to start working on an existing GitHub project.
Clone into a new folder — never inside an existing repo.