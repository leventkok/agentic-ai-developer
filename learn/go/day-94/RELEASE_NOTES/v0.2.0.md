# Release v0.2.0

**Date:** 2026-08-31

## Highlights

- Cache-aside with Redis / in-memory TTL
- Domain events via outbox + idempotent worker
- pprof profiling and benchmark regression guard
- Team DX: Makefile, OpenAPI, PR template, changelog

## Upgrade notes

- New env vars: `REDIS_URL`, `NATS_URL`, `PPROF_PORT`, `OUTBOX_WORKERS`
- Run migrations 007–008 for outbox and processed_events tables
- Worker process recommended when `NATS_URL` is set

## Verify

```powershell
./scripts/verify-performance.ps1
./scripts/verify-cache-messaging.ps1
```
