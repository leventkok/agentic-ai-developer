# Day 80 — Containers & CI/CD Capstone

**Phase:** Containers & CI/CD (Days 76–80)

Docker image, Compose stack, full GitHub Actions pipeline, and deploy docs.

See `CAPSTONE.md` and `DEPLOY.md`.

## Quick start

```powershell
cd learn/go/day-80
.\scripts\verify-deploy.ps1
docker compose up --build
```

Version: `v0.1.0` (`VERSION` file).
