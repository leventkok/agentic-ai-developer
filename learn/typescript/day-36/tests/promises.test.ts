import { describe, it } from "node:test";
import assert from "node:assert/strict";

// TODO Task 3-4: Add tests once functions are implemented.
// See README.md

describe("Promise types", () => {
  it("placeholder — replace after implementing basic.ts", () => {
    assert.ok(true);
  });
});
