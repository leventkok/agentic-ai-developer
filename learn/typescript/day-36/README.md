# Day 36 — Promise Types

**Phase:** Async, Promises & Errors (Days 36–40)

## Setup
```powershell
cd learn/typescript/day-36
npm install
```

## Tasks — which file to edit

| Task | What | File |
|------|------|------|
| **1** | Type `Promise<User>` and `Promise<void>` | `src/promises/basic.ts` |
| **2** | Chain with `.then` / `.catch` | `src/promises/basic.ts` |
| **3** | `Promise.all` + `Promise.allSettled` | `src/promises/parallel.ts` |
| **4** | No floating promises | `src/index.ts` |
| **5** | Tests | `tests/promises.test.ts` |

**Read-only today:** `src/types/user.ts`

See chat lesson for full code + logic per task.
