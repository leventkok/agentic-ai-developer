# Day 41 — lib.dom and Global Types

**Phase:** DOM & Browser TypeScript (Days 41–45)

## Focus today
- `"lib": ["ES2022", "DOM"]` in tsconfig
- Typed queries in `src/dom/query.ts`
- Null narrowing with `requireElement<T>()`

## Run
```powershell
cd learn/typescript/day-41
npm install
npm test
```

See `day-45` README for full phase map — days 41–45 share this todo app codebase.
