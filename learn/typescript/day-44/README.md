# Day 44 — fetch and Typed Responses

**Focus:** `src/api/client.ts` — typed fetch, error unions, AbortSignal, runtime shape check.

```powershell
cd learn/typescript/day-44
npm install && npm test
```
