# Day 48 — HTTP Servers

**Focus:** `src/server/http-server.ts`, `src/handlers/notes.ts` — typed routes, JSON bodies, error responses.

```powershell
cd learn/typescript/day-48
npm install && npm test
```
