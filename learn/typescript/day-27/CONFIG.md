# tsconfig option map

## App (`tsconfig.json`)
- TODO: document why each option matters for a Node app

## Library (`tsconfig.lib.json`)
- TODO: document why `declaration: true` and separate `outDir` matter

## Base (`tsconfig.base.json`)
- TODO: list shared options inherited by both configs
