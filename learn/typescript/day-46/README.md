# Day 46 — Node Project Setup

**Focus:** `package.json`, `nodenext`, npm scripts (`dev`, `build`, `test`).

```powershell
cd learn/typescript/day-46
npm install && npm test
```

See `day-50/README.md` for full Node phase map.
