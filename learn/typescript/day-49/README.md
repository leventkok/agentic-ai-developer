# Day 49 — Environment Variables & Structure

**Focus:** `src/config/env.ts` — typed `process.env`, fail-fast validation, layered folders.

```powershell
cd learn/typescript/day-49
npm install && npm test
```
